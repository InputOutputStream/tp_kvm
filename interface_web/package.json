{ 
  "name": "thoth-cloud-manager",
  "version": "2.0.0",
  "description": "Interface web avancée pour gérer les VMs KVM/libvirt avec monitoring temps réel",
  "main": "back/server.js",
  "scripts": {
    "start:open": "node back/server.js & sleep 2 && xdg-open http://localhost:3000",
    "dev": "nodemon back/server.js",
    "setup": "npm install && echo 'Installation terminée! Configurez sudo pour virsh'"
  },
  "keywords": ["kvm", "libvirt", "vm", "virtualization", "monitoring", "vnc"],
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "libvirt": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}

